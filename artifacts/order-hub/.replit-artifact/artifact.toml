kind = "web"
previewPath = "/"
title = "Order Hub"
version = "1.0.0"
id = "artifacts/order-hub"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 24028

[services.development]
run = "pnpm --filter @workspace/order-hub run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/order-hub", "run", "build" ]
publicDir = "artifacts/order-hub/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "24028"
BASE_PATH = "/"
